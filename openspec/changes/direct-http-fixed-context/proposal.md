# Direct HTTP fixed-context MVP

用户授权：在当前环境完成纯 HTTP 可用路径和交付，不再将协议侦察或浏览器常驻作为前置研发。

新增 direct 模块与命令：保留完整单次 cURL bootstrap、heartbeat、start once、rolling status、私有日志、loopback API、明确失败。既有 browser/core 保留，不声称高级 capability 被验证。

非目标：自动资源创建、认证绕过、SSE、多模型、工具桥接、多账号。
